Hover/focus tooltip — wraps a trigger and shows a small dark bubble.

```jsx
<Tooltip label="Archive" placement="bottom">
  <IconButton label="Archive"><i data-lucide="archive"></i></IconButton>
</Tooltip>
```

Placement: `top` / `bottom` / `left` / `right`.
