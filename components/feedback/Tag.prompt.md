A chip for labels, categories, and filters — bordered, optionally with a color dot, removable, or selectable.

```jsx
<Tag color="var(--terracotta-500)">Writing</Tag>
<Tag onRemove={() => drop(id)}>research</Tag>
<Tag selectable selected onClick={toggle}>Today</Tag>
```
