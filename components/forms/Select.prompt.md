Native `<select>` styled as a Fabric field with a custom chevron.

```jsx
<Select label="Sort by" options={['Recent', 'Name', 'Priority']} />
<Select placeholder="Choose a list" options={[{value:'1',label:'Today'},{value:'2',label:'Later'}]} />
```
