Checkbox with a custom check glyph and optional label + description.

```jsx
<Checkbox label="Remind me" description="A gentle nudge at 9am" defaultChecked />
<Checkbox label="Archive when done" />
```
