A hairline rule. Vertical needs a flex parent with height. Pass `label` for a centered text-in-rule divider, or `variant="stitch"` for a dashed running-stitch thread — a textile detail.

```jsx
<Divider />
<Divider variant="stitch" />
<Divider orientation="vertical" />
<Divider label="or" />
```
