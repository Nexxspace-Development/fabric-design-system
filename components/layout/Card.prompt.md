The fundamental Fabric surface: warm paper, hairline border, soft shadow, generous radius.

```jsx
<Card elevation="raised" padding="md">
  <h3>Morning pages</h3>
  <p>Three things to focus on today.</p>
</Card>
<Card interactive elevation="flat" as="a" href="#">Open project →</Card>
```

`elevation`: `flat` / `raised` / `floating`. `padding`: `sm` / `md` / `lg`. Set `interactive` for clickable lift.
