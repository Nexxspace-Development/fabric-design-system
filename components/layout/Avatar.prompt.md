Circular avatar — shows the image when `src` is provided, otherwise colored initials from `name` (tone is hashed from the name across the muted palette).

```jsx
<Avatar name="Mara Iversen" size="md" />
<Avatar name="Theo" src="/people/theo.jpg" size="lg" ring />
```
